name: posics-setup
channels:
  - defaults
  - conda-forge
dependencies:
  - json
  - matplotlib
  - numpy
  - pip
  - python=3.10
  - pyvisa
  - tk
  - tqdm
  - pip:
      - picosdk
      - pyqt5
      - pystage-apt
      - thorlabs-apt-device